
Double (x){
    x:=(x*2);
    return x;
}

Bruh(x, d,y){
if (x <= d)
    then d :=x;
    else while (d < x) {
        d:=(d+x);
        x:=(x+4);
        d:=(d-y);
    }; 
return d;
}

Bruh[Double[2],Double[3],1]; -- essentially bruh[4,6,1] should return 4 as (2*2 < 3*2)