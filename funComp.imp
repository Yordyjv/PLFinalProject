
Double (x){
    x:=(x*2);
    return x;
}

Bruh(x, d,y){
if (x <= d)
    then d := Double[d];
    else while (d < x) {
        d:=(d+x);
        x:=(x+4);
        d:=(d-y);
    }; 
return d;
}

Bruh[7,3,1];